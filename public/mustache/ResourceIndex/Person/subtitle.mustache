{{#if countryChampionFor}}

    <span>
        {{i18n (stringFormat "%s.countryChampionFor" @type)}}
        {{#countryChampionFor}}
            <a href="/country/{{.}}">{{i18n . bundle="iso3166-1-alpha-2"}}</a>{{#unless @last}},{{/unless}}
        {{/countryChampionFor}}
    </span>

{{else}}

    {{! just show country, if not a countrychampion – could be more precise: show if they defer ... }}

    {{#if location.address.addressCountry}}
        <span>
            <a href="/country/{{location.address.addressCountry}}">{{i18n location.address.addressCountry bundle="iso3166-1-alpha-2"}}</a>
        </span>
    {{/if}}

{{/if}}

{{#if additionalType}}
    <span>
        {{i18n "additionalType"}}:
        {{#additionalType}}
            {{.}}{{#unless @last}},{{/unless}}
        {{/additionalType}}
    </span>
{{/if}}
