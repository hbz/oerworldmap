{{#property}}
<fieldset>

  <legend>Geo Coordinates</legend>
  {{input (stringFormat "%s[lat]" property_path) lat}}
  {{input (stringFormat "%s[lon]" property_path) lon}}

</fieldset>
{{/property}}
{{^property}}
    <fieldset>

        <legend>Geo Coordinates</legend>
        {{input (stringFormat "%s[lat]" property_path) lat}}
        {{input (stringFormat "%s[lon]" property_path) lon}}

    </fieldset>
{{/property}}
