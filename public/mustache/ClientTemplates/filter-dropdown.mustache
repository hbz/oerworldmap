<div class="btn-group dropdown filter" data-filter-name="{{name}}">

    {{#if small}}

        <button
            type="button"
            class="btn btn-default dropdown-toggle {{#if active}} active {{/if}}"
            title="{{button_title}}"
            data-toggle="dropdown"
        >
            <span class="text" style="margin-right: 10px;"><i class="fa fa-{{button_icon}}"></i></span>
            <i class="fa fa-chevron-down"></i>
        </button>

    {{else}}

        <button
            type="button"
            class="btn btn-default {{#if active}} active {{/if}}"
            title="{{button_title}}"
            data-filter-action="toggle-filter"
        >
            <span class="text">{{{button_text}}}</span>
        </button>

        <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
            <i class="fa fa-chevron-down"></i>
        </button>

    {{/if}}

    {{#if typeahead}}

        <div class="dropdown-menu dropdown-menu-right dropdown-menu-typeahead">
            <button type="button" class="btn btn-warning" data-filter-action="apply-filter">{{i18n "ClientTemplates.filter-dropdown.applyFilter"}}</button>
            <div style="position: relative;">
                <input type="text" class="form-control typeahead" placeholder="..." />
                <div class="input-icon">
                    <i class="fa fa-search"></i>
                </div>
            </div>
        </div>

    {{else}}

        <div class="dropdown-menu dropdown-menu-right">
            <button type="button" class="btn btn-warning" data-filter-action="apply-filter">{{i18n "ClientTemplates.filter-dropdown.applyFilter"}}</button>

            {{#each buckets}}

                <div class="tt-suggestion tt-selectable">
                    {{> filter-dropdown-entry}}
                </div>

            {{else}}

                <div class="tt-suggestion">
                    <em>{{i18n "ClientTemplates.filter-dropdown.suggestionTitle"}} <strong>{{button_text}}</strong></em><br/>
                    <small>{{i18n "ClientTemplates.filter-dropdown.suggestionMessage"}}</small>
                </div>

            {{/each}}

        </div>

    {{/if}}

</div>
