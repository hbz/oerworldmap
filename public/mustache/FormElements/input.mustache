<div class="resource-form-widget{{#if hidden}} hidden{{/if}}" data-behaviour="input">

    <h4>{{title}}{{#tooltip}} <i class="fa fa-question-circle" title="{{.}}"></i>{{/tooltip}}</h4>

    {{#if multiple}}
        <ul title="{{title}}" class="multiple-list">
            {{#property}}
                <li class="multiple-one">{{input (stringFormat "%s[%s]" key @index) . type=input_type}}</li>
            {{/property}}
            <li class="multiple-one">{{input (stringFormat "%s[%s]" key (size property)) "" type=input_type}}</li>
        </ul>
    {{else}}
        <div class="single">
            {{input key property type=input_type}}
        </div>
    {{/if}}

</div>
