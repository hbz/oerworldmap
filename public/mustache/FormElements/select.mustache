<div class="resource-form-widget" data-behaviour="select" data-options="{{options}}" data-key="{{property_path}}">

    <h4>{{title}}{{#tooltip}} <i class="fa fa-question-circle" title="{{.}}"></i>{{/tooltip}}</h4>

    {{#if multiple}}
        <ul title="{{title}}" class="multiple-list">
            {{#property}}
                <li class="multiple-one">{{input (stringFormat "%s[%s]" property_path @index) . type=type}}</li>
            {{/property}}
            <li class="multiple-one">{{input (stringFormat "%s[%s]" property_path (size property)) "" type=type}}</li>
        </ul>
    {{else}}
        <div class="single">
            {{input property_path property type=type}}
        </div>
    {{/if}}

</div>
